

## 我想要做一个番茄时钟的I0S APP，我准备让Cursor 用HTML的形式 帮我模拟出UI/UX 的设计。
我希望你可以帮我生成一个规范的提示词，让我用来和Cursor沟通。

Agent 
```
番茄时钟iOS应用UI/UX设计提示词
请帮我设计一个简约风格的番茄时钟iOS应用UI/UX原型。我需要使用HTML/CSS来呈现设计，以便直观地展示界面和交互方式。
核心需求
设计风格：简约现代、符合iOS设计规范
主色调：番茄红(#FF6B6B)为主色，搭配浅色背景
界面布局：清晰直观，操作简单
字体：使用iOS系统字体(-apple-system, SF Pro Display)
响应式元素：所有交互元素尺寸适中，便于触控
需要包含的页面（在同一HTML页面平铺展示）
专注计时页面（已有）
大型圆形计时器显示剩余时间
开始/暂停/重置控制按钮
专注/短休息/长休息模式切换
当前任务简略展示
任务管理页面（已有）
任务列表（进行中/已完成）
任务添加按钮
日期选择器
统计分析页面（已有）
每周/月专注时间统计
专注类别分布图表
完成任务数据图表
设置页面（已有部分）
时间设置（专注/休息时长）
通知设置
主题设置
声音设置
新增页面需求：
添加任务页面：表单界面，包含任务名称、类别选择、预计番茄钟数量、日期选择、优先级设置
专注进行中页面：大型倒计时界面，带暂停/放弃按钮，当前任务名称显示
休息中页面：休息倒计时，温馨提示，跳过休息选项
任务完成页面：成就展示，包含统计数据和激励文案
交互细节
所有按钮点击应有视觉反馈
重要操作需要二次确认
任务完成时应有愉悦的视觉反馈
底部tab栏用于主要页面切换
添加明确的返回导航
考虑手势操作（如滑动切换任务）
其他要求
使用纯HTML/CSS实现，无需JavaScript功能
在设计中加入iOS风格元素（状态栏、底部指示条等）
界面要保持一致的设计语言和视觉风格
每个页面都应清晰标记其用途
平铺展示所有页面设计，方便一目了然地比较
请在现有HTML页面基础上继续完善设计，保持风格统一，并添加剩余的关键页面。
```

Ask
```
我需要你帮我设计一个简约而美观的番茄时钟iOS应用的UI/UX，请用HTML和CSS模拟出设计效果图。

设计要求：
1. 风格要求：遵循iOS设计规范，简约现代，视觉清爽，使用舒适的配色方案
2. 主色调建议：使用番茄红色为主色，搭配中性色和柔和的辅助色
3. 排版：清晰的层次结构，易于阅读的字体大小
4. 交互：直观简单，一目了然的操作逻辑

请设计以下核心页面并在一个HTML页面中平铺展示：
1. 主计时页面：大型圆形计时器，开始/暂停/重置按钮，不同时间模式切换（专注、短休息、长休息）
2. 任务管理页面：任务列表、添加任务按钮、完成/未完成任务区分
3. 统计分析页面：专注时间统计、每日/每周趋势图表、完成任务数量
4. 设置页面：时间长度设置、通知设置、主题设置、声音设置等

每个页面都需要包含：
- iOS状态栏
- 清晰的标题和导航元素
- 底部标签栏（用于切换不同页面）
- 符合iOS交互习惯的UI组件

请用HTML和CSS实现这些设计，使其看起来像真实的iOS应用界面。您可以先创建一个页面，然后我会要求您继续设计其他页面。每个设计元素都应该考虑实用性和美观性的平衡。
```


## 我希望你可以多帮我对设计提一下要求，设计一定要美观大方，让人觉得舒服

```
请帮我设计一个高品质的番茄时钟iOS应用UI/UX，需要体现出精致、专业和易用性。

设计细节要求：

1. 视觉风格：
   - 采用现代简约设计，减少视觉干扰
   - 注重留白，创造优雅舒适的视觉体验
   - 使用柔和的阴影和渐变，增加层次感
   - 所有圆角保持统一（建议16px），体现设计一致性

2. 配色方案：
   - 主色调：使用温暖的番茄红（建议#FF6B6B），让人感觉积极向上
   - 背景色：浅灰白色（建议#FAFAFA），减少视觉疲劳
   - 强调色：根据不同功能区使用适当的辅助色
   - 文字层级：标题用深色（#333），副文本用灰色（#666，#999）

3. 交互设计：
   - 所有按钮和可点击区域要有明确的视觉反馈
   - 动效要柔和自然（可用CSS transition表现）
   - 重要操作按钮要醒目但不突兀
   - 手势操作区域要足够大（建议至少44px）

4. 布局要求：
   - 遵循iOS间距规范，保持统一的边距（建议20px）
   - 内容区块之间留有合适的空间（建议25px）
   - 信息层级清晰，重要内容突出显示
   - 保持垂直和水平对齐

5. 功能细节：
   计时界面：
   - 大型圆形计时器要成为视觉焦点
   - 时间显示要醒目清晰（建议使用大字号SF Pro Display字体）
   - 进度条动效要流畅优雅
   - 控制按钮要醒目但不喧宾夺主

   任务列表：
   - 任务卡片要有适当的阴影和圆角
   - 完成状态要有明确但不夸张的视觉区分
   - 添加任务按钮要容易识别
   - 列表间隔要适中，保持通透感

   数据统计：
   - 图表要简洁清晰，避免过度装饰
   - 重要数据要突出显示
   - 使用柔和的色彩展示不同类别
   - 趋势图要易于理解

   设置界面：
   - 分类清晰，层次分明
   - 开关等控件要符合iOS风格
   - 选项说明要简洁明了
   - 保持视觉简洁性

6. 细节提升：
   - 使用适当的毛玻璃效果
   - 加入精致的图标设计
   - 注意深色模式适配
   - 适当使用微动效提升体验

7. 可用性考虑：
   - 所有文字要清晰可读
   - 重要操作要有确认机制
   - 提供足够的视觉反馈
   - 考虑单手操作的便利性

请确保设计既美观又实用，让用户能够轻松愉悦地使用这款应用。每个设计元素都应该有其存在的意义，避免纯粹装饰性的元素。
```

## 我希望最终所有APP 界面都可以在index.html 平铺展示，然后Cursor在生成的过程中，每次可以生成一
部分，避免内容太长 太多，产生中断

```
请帮我设计一个番茄时钟iOS应用的UI/UX界面，并将所有设计页面平铺展示在一个index.html文件中。

为避免生成过程中断，请按以下步骤分阶段完成：

第一阶段：基础框架
- 创建index.html文件，设置页面标题为"番茄时钟APP设计展示"
- 添加基础CSS样式，包括页面布局、设备外观样式
- 创建一个容器，用于平铺展示多个设备界面
- 设置响应式布局，确保在不同屏幕尺寸下都能良好展示

第二阶段：主计时页面
- 在第一个设备容器中创建主计时界面
- 包含大型圆形计时器、控制按钮和模式切换
- 添加今日任务预览区域

第三阶段：任务管理页面
- 添加第二个设备容器，创建任务管理界面
- 设计任务列表、分类和添加任务按钮

第四阶段：统计页面
- 添加第三个设备容器，创建数据统计界面
- 设计专注时间统计图表和任务完成情况

第五阶段：设置页面
- 添加第四个设备容器，创建设置界面
- 设计各种配置项和选项

设计要求：
- 简约现代设计风格，主色调使用番茄红(#FF6B6B)
- 统一的视觉语言，包括字体、间距和圆角
- 每个设备界面都包含状态栏和底部导航栏
- 所有界面元素要符合iOS设计规范

重要说明：
- 每次请只生成一个阶段的代码
- 每生成一个阶段后，我会确认，然后你再继续下一阶段
- 确保每个阶段的代码都在同一个index.html文件内
- 后续阶段应该在已有代码基础上添加新内容，而不是重写整个文件
- 使用注释标记每个阶段的开始和结束，便于理解和修改

请先生成第一阶段的代码，等我确认后再继续。
```


### 确认，继续

[image.png](image.png)

### 确认，继续

[image2.png](image2.png)

### 确认，继续
[image3.png](image3.png)

### 确认，继续
[image4.png](image4.png)


